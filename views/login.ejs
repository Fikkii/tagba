<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/style.css">
    <title>Welcome!!!</title>
</head>
<body>
    <div class="flex-container">
        <div class="image-contianer"><img src="https://tncbcagege.org/wp-content/uploads/2022/10/cropped-logo-1.png" alt=""></div>
        <div class="logo">WELCOME TO THE CHURCH CENTER</div>
        <form action="/login" method="POST">
            <small><em style="color: red;">
                <% if(typeof error == 'undefined' || error.length == 0){%>
                    <strong style="color: gray;">Please enter your username and password</strong>
                <%}else{%>
                    <%= error[0] %>
                <% } %>
            </em></small>
            <input type="text" name="username" placeholder="Username" required>
            <input type="password" name="password" placeholder="Password" required>
            <button type="submit">Login...</button>
        </form>
    </div>
</body>
</html>